<Window x:Class="Server.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Server"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <CollectionViewSource x:Key="AllGroups" Source="{Binding Groups}" />
    </Window.Resources>
    
    
    <Grid>
        <!--Source={StaticResource AllGroups}}"-->

            
        <DataGrid HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding Users}" CanUserAddRows="False" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Header="Имя" Binding="{Binding Path=Login}"/>
                <DataGridComboBoxColumn SelectedValuePath="{Binding Name}" SelectedValueBinding="{Binding Group}" DisplayMemberPath="Name" ItemsSource="{Binding Source={StaticResource AllGroups}}"></DataGridComboBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
        
        
    </Grid>
</Window>